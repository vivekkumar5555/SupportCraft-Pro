{
  "name": "whitelabel-ai-support-widget",
  "version": "1.0.0",
  "description": "White-label AI support widget with MERN stack",
  "main": "index.js",
  "scripts": {
    "dev": "concurrently \"npm run backend:dev\" \"npm run admin:dev\"",
    "backend:dev": "cd backend && npm run dev",
    "admin:dev": "cd admin-frontend && npm run dev",
    "build": "npm run backend:build && npm run admin:build && npm run widget:build",
    "backend:build": "cd backend && npm run build",
    "admin:build": "cd admin-frontend && npm run build",
    "widget:build": "cd widget && npm run build",
    "install:all": "npm install && cd backend && npm install && cd ../admin-frontend && npm install && cd ../widget && npm install"
  },
  "keywords": ["ai", "chatbot", "support", "widget", "mern"],
  "author": "",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
